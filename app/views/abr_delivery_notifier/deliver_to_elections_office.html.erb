<style type="text/css">
  <!--
  .boxtext {
    font-family: "Arial Black", Gadget, sans-serif;
    font-size: 22px;
    font-style: normal;
    line-height: 1.5em;
    font-weight: bolder;
    text-transform: uppercase;
  }
  -->
</style>

<%- if Rails.env.staging2? || Rails.env.development? %>
DEBUG: Real recipient would be: "<%= @abr.home_state.counties[@abr.registration_county][:abr_email_address] %>"
<%- end %>

<%= I18n.t('email.abr.deliver_to_elections_office.body', 
  :pdf_url=>@pdf_url, 
  :election_office_name => @election_office_name,
  :abr_first_name => @abr_first_name,
  :abr_last_name => @abr_last_name,
  :state_name=> @abr_home_state_name,
  :home_state_email_instructions => @home_state_email_instructions,
  :registrar_address=>@registrar_address,
  :cancel_reminders_url=>@cancel_reminders_url,
  :registrar_phone=>@registrar_phone,
  :registrar_url=>@registrar_url, :locale => @locale).html_safe %>

<hr size="3" align="left" width="100%" color="black"><br><br>

<%= I18n.t('email.abr.deliver_to_elections_office.footer', :locale => @locale).html_safe %>