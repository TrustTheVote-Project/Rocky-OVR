%div.whitelabeled
  %ul.stacked
    %li.width_e
      %h3 New asset
      %div= f.file_field :file
  %br
  - assets = PartnerAssetsPresenter.new(@partner)
  %fieldset
    %legend Not approved
    %table.assets
      %tbody
        - if assets.not_approved.empty?
          %tr
            %td.name There are no assets yet
        - else
          - assets.not_approved.each do |asset|
            %tr
              %td.name
                = check_box_tag "remove[#{asset}]"
                = asset
          %br
          %div Click 'Update' to delete selected items
  %fieldset
    %legend Approved
    %table.assets
      %tbody
      - if assets.approved.empty?
        %tr
          %td.name There are no approved assets yet

      - assets.approved.each do |asset|
        %tr
          %td.name= asset
    %br
