#progress-bar
  %ol
    %li.progress-title= t('txt.registration.progress_indicator').html_safe
    = progress_indicator(@num_steps).html_safe

%h1= t("states.custom.pa.#{params[:step]}_header").html_safe
